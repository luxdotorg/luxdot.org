# LuxDot / نقطة نور — v2.9.4 CLEAN MEDIA HOTFIX
Date: 13 August 2026

## Fixed
- Removed the old multi-dot photon swarm from active code.
- Book opening now uses 3 soft light rays and only 7 slow drifting dust motes.
- Thumbnail assets are now raster PNG files and resolve relative to the root UI script, including nested pages.
- Added a local image fallback so broken image icons are never shown.
- Localized the three remote Memory Atlas images into `assets/images/memory/`.
- Converted shared-library cover placeholders to local PNG assets for more reliable rendering.
- Cache-busted active CSS/JS references to `v=294`.
- Removed old deployment JS/CSS under `docs/legacy` and old build-history clutter under `docs/history`.

## Scope
Reliability/cleanup hotfix only. No new research content.

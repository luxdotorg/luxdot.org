# LuxDot v4.3.5 — Home Parity / Header / Icons

- Unified header navigation across all site languages; Faith & Wisdom and Nusantara now appear outside Arabic too.
- English LuxDot brand retained as LuxDot.
- Home cards now use Arabic-master interaction in every locale: whole cartouche clickable, separate Open buttons hidden.
- Semantic corner icons are assigned by destination/href instead of translated text, so the same icon appears in every language.
- Duplicate corner artwork is removed before one semantic icon is inserted, preventing overlapping double symbols.
- Mobile card interaction and focus states preserved.

# LuxDot v4.3.71 — Map Reliability Repair

- Reiderland historical map is local in AR/EN/NL.
- Reiderland interactive map cartridge now uses native HTML details/summary; opening no longer depends on JavaScript event binding.
- Reiderland place shortcuts target the embedded OpenStreetMap frame directly.
- Projects Circle of Care links carry the active language and are hardened for pointer/click behavior.
- Circle of Care no longer depends on external Leaflet JS/CSS. It uses an embedded OpenStreetMap map and local care-point data.
- Care cards update/focus the map with dependency-free local JavaScript.
- Cache versions bumped to 4371 on repaired assets.

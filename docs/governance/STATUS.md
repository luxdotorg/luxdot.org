# Governance v1 Status

Candidate branch: `luxdot/governance-v1`

State: ready for pull-request validation.

The candidate intentionally changes repository governance/QA files only. It does not modify production website runtime files. The next gate is GitHub's pull-request workflow execution and diff review; production branch protection should be configured after stable required-check names are observed.

# Maintainer Quick Start

For routine LuxDot work:

`main` → create `luxdot/*` branch → make scoped change → open PR → let QA run → review diff → merge according to risk class.

Do not bypass the process for convenience. HIGH-risk infrastructure/security/database changes stop before production merge until explicit human approval is recorded.

See `GOVERNANCE.md` at repository root for the authoritative policy.

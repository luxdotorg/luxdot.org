# Merge Rule

No governance candidate is considered active merely because it exists on a branch. It becomes repository policy after review and merge to `main`, followed by configuration of applicable GitHub repository protections.

# Known Governance Items

## Workflow consolidation
Governance v1 currently contains an initial comprehensive QA workflow plus a smaller baseline static QA workflow and focused credential guard. After the first PR runs against the legacy repository, consolidate overlapping checks based on observed results rather than guessing about legacy compatibility.

This item does not affect production site code. It exists to keep the first governance rollout auditable and reversible.

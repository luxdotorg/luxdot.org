# Ownership Model

`CODEOWNERS` currently uses the repository owner account as the baseline owner. As LuxDot gains maintainers, replace or extend this with named GitHub users or organization teams and use branch/ruleset settings to require appropriate review.

Ownership is not a substitute for automated checks, and automated checks are not a substitute for human approval of HIGH-risk changes.

# Pull Request Checks

Governance v1 introduces lightweight checks before heavier tooling:

- static repository sanity;
- JavaScript syntax parsing;
- HTML parser sanity;
- focused private-credential detection.

A green check means these specific tests passed; it does not certify factual accuracy, accessibility, browser compatibility, security correctness or deployment success.

# No Routine Direct Writes to `main`

Routine LuxDot development uses pull requests. Technical permission to write directly to `main` is not project authorization to do so.

Emergency exceptions should be rare, documented and followed by an audit/review of the resulting production state.
